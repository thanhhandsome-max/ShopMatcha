# Copy this file to `.env` and adjust values as needed.
# DO NOT commit `.env`.

# Prisma / MySQL
# Using the provided docker-compose MySQL (root/root, db=matcha_shop)
DATABASE_URL="mysql://root:root@localhost:3306/matcha_shop"
